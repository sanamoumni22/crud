process.env.NODE_ENV = 'test';

require('./mocks/mongodb-connect');

let config = require('./config/config-mongoose');
let crud = require('../index');
let crudTest = require('./test.crud');
let todos = require('./mocks/models/todos');

let oCrud = new crud(config);

crudTest(oCrud, 'Mongoose', 'MongoDB', todos, 'todos');