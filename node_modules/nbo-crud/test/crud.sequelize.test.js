process.env.NODE_ENV = 'test';
let config = require('./config/config-sequelize');
let crud = require('../index');
let crudTest = require('./test.crud');
let foo = require('./mocks/models/foo');

let oCrud = new crud(config);

foo.sync({force: true});

crudTest(oCrud, 'Sequelize', 'SQLite', foo, 'foo');