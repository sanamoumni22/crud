let mongoose = require('mongoose');

let config = {
  db: 'mongodb://mongo:27017/test_node_crud'
};

// connect to mongodb
try {
  mongoose.connect(config.db);
}catch(err) {
  mongoose.createConnection(config.db);
}
