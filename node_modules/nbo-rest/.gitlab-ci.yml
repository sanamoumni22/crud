image: node:10

stages:
  - build
  - test

cache:
  paths:
    - node_modules/

install_dependencies:
  stage: build
  script:
    - npm i
  artifacts:
    paths:
      - node_modules/

tests:
  stage: test
  script: npm test

services:
  - nouchka/sqlite3:latest
  - mongo:3.6.3
