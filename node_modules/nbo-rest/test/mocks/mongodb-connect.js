let mongoose = require('mongoose');
mongoose.Promise = require('bluebird');

let config = {
  db: 'mongodb://mongo:27017/test_node_rest'
};

// connect to mongodb
try {
  mongoose.connect(config.db);
}catch(err) {
  mongoose.createConnection(config.db);
}

