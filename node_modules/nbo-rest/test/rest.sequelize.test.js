let config = require('./config/config-sequelize');
let expressMock = require('./mocks/router');
var oRequest = require('./mocks/request')();
let rest = require('../index');
let restTest = require('./test.rest');
let foo = require('./mocks/models/foo');

let oRest = new rest(expressMock, {}, config);

oRequest.user[config.entities.foo.primary_key_name] = 123;

foo.sync({force: true});

restTest(oRest, 'Sequelize', 'SQLite', oRequest, foo, 'foo');